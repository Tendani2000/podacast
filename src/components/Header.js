import React from "react";
import "./app.css"

export default function Header (){
    return(
        <header className="header">
        <h2 className="header--title">Podcasts Beyond</h2>
        <h4 className="favorite">Favorites</h4>
        </header>
    )
}